{
  "name": "mksport-site",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
"scripts": {
  "dev": "tinacms dev -c \"astro dev\"",
  "build": "astro build",
  "build:tina": "tinacms build && astro build",
  "preview": "astro preview",
  "astro": "astro"
},
  "dependencies": {
    "astro": "^6.1.2",
    "tinacms": "^3.7.5"
  },
  "devDependencies": {
    "@tinacms/cli": "^2.2.5",
    "@types/node": "^25.6.0"
  }
}
